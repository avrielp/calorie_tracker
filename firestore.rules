rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This project is designed to sync through Cloud Functions (not direct Firestore access).
    // Lock everything down by default.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}


